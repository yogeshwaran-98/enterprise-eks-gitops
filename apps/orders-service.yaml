apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: orders-service
  namespace: argocd

spec:
  project: default

  sources:
    - repoURL: https://github.com/yogeshwaran-98/enterprise-eks-platform.git
      targetRevision: main
      path: helm/orders-service

      helm:
        valueFiles:
          - $values/environments/production/orders-service-values.yaml

    - repoURL: https://github.com/yogeshwaran-98/enterprise-eks-gitops.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: enterprise

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
